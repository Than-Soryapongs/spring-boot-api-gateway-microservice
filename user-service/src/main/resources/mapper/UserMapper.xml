<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pongs.user_service.mapper.UserMapper">

    <insert id="insert"
            parameterType="com.pongs.user_service.model.entity.User"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO users (
            username,
            first_name,
            last_name
        )
        VALUES (
            #{username},
            #{firstName},
            #{lastName}
        )

    </insert>

    <select id="findByUsername"
            parameterType="string"
            resultType="com.pongs.user_service.model.entity.User">

        SELECT
            id,
            username,
            first_name AS firstName,
            last_name AS lastName,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM users
        WHERE username = #{username}

    </select>
</mapper>